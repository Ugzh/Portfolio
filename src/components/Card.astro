---
import Tag from './Tag.astro';

interface Props {
	title: string;
	description: string;
	href: string;
	src: string;
	alt: string;
	techno: string;
}

const { href, title, description, techno, src, alt } = Astro.props;
---

<div class="w-full border border-slate-400 rounded-3xl p-6 flex">
	<a href=`${href}` class="flex gap-10" target="_blank">
		<img src=`${src}` alt=`${alt}` class="max-w-80 max-h-80 w-full rounded-2xl">
	<div class="space-y-10">
		<div class="space-y-2">
			<h4 class="text-2xl underline">{title}</h4>
			<p>{description}</p>
		</div>
		<div class="flex w-full flex-col gap-4"> 
			<h4 class="underline-offset-3 underline text-lg">Technologies</h4>
			<div class="flex gap-2">
				<Tag techno={techno}></Tag>
			<slot />
			</div>
		</div>
	</div>
	</a>
</div>